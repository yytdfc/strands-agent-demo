

## playwright-mcp Build Steps

``` bash
# Clone the repository:
git clone https://github.com/yytdfc/playwright-mcp.git -b dev
cd playwright-mcp

# Install dependencies:
npm install

# Build the project:
npm run build

# Create a local link:
npm link
```
